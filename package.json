{
  "name": "antiengine",
  "version": "2.0.0",
  "description": "Antigravity Engine Plugin",
  "scripts": {
    "build": "esbuild core/mcp-server/index.ts --bundle --platform=node --outfile=dist/mcp-server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.3"
  }
}
